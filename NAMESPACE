# This course carries no R code; the package is a thin dependency manifest only
# (see DESCRIPTION). An (effectively empty) NAMESPACE is required for it to build
# and install so its dependencies resolve.
